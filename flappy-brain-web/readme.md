Option B: Dùng VSCode

Mở thư mục flbb trong VSCode

Mở Terminal tích hợp (Ctrl+`)

Chạy backend:

cd D:\develop\flbb\flappy-brain-web\flappy-brain-backend

node server.js

Mở Terminal mới (Ctrl+Shift+`)

Chạy frontend:

npx live-server --port=5500

Ctrl+Click link để mở game
